# Database Configuration
# Get these from your Supabase Database Settings
# DATABASE_URL should use the pooler host (contains "pooler" and port 6543)
DATABASE_URL="postgresql://postgres.[project]:[password]@aws-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"
# DIRECT_URL should use the direct session host (port 5432)
DIRECT_URL="postgresql://postgres.[project]:[password]@aws-[region].pooler.supabase.com:5432/postgres"

# Authentication
# Generate a secure random secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="your-64-character-random-secret-key-here"

# Environment
NODE_ENV="production"


